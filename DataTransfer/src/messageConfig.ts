// Use this config to specify data we want the Front End to have for a specific sensor reading
// Key should be the message Id, value should be the packet to extend the message with
// i.e.
/*

"02819": {
    "visualizationType": "line"
}

*/
const MsgConfig = {

};

export default MsgConfig;